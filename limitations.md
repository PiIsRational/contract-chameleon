##

CC only

- Java: there can't be multiple out parameter
- There should only be at most one (inout) parameter \[this\].

Encapsulation Levels:

1. observable encapsulated

- the interaction is separated, but the data below might be shared

1. method encapsulated (method wise)

- on the call and exit the encapsulation properties are ensured

1. splitable encapsulation (verifast)

1. static encapsulation (strict encaps.)

- all encapsulation properties are always ensured

1. ownership transfer

1 -> 2 -> 4
3
